# parallel-kmeans
Parallelized the kmeans algorithm using TBB, Streaming SIMD Extensions, and cache optimizations.  

Additionally in this repo is an exploration of different load balancing approaches on a prime checker algorithm.

# See `analysis.pdf` for a full writeup

